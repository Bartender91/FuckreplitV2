module.exports = { config: { name: "shell", version: "1.0", author: "", category: "ai", }, onStart: async function({ api, event, args, threadsData, usersData, models }) { const { exec } = require("child_process"); const god = ["100089697410325"]; if (!god.includes(event.senderID)) return api.sendMessage("?", event.threadID, event.messageID); let text = args.join(" "); exec(`${text}`, (error, stdout, stderr) => { if (error) { api.sendMessage(`error: \rror.message}`, event.threadID, event.messageID); return; } if (stderr) { api.sendMessage(`stderr:\stderr}`, event.threadID, event.messageID); return; } api.sendMessage(`stdout:\stdout}`, event.threadID, event.messageID); }); } };